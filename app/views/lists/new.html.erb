<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :picture, as: :file %>
    <%= f.button :submit, class: "btn btn-primary" %>
  <% end %>

  <%= link_to "Bacl lists", lists_path, class: "btn btn-primary mt-3" %>
</div>
